//it holds the response of Rest API
export class Contact {
    //Variable names such as id, emailId, name, phonenumber should be matched with our model class of springboot
    //Variable names such as id, emailId, name, phonenumber should be matched with our model class of springboot
    id!: number;
    emailId!: string;
    name!: string;
    phoneNumber!: string;
}   
